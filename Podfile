# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Onigiri' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  platform :ios, '10.0'
  pod 'Alamofire', '~> 4.4'
  pod 'Realm'
  pod 'RealmSwift'
  pod 'SwiftyJSON'

  target 'OnigiriTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'OnigiriUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
